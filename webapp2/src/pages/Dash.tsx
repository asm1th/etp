import React, { FC } from "react";
import PostContainer from "../components/PostContainer";

const Dash: FC = () => {
    return (
        <div>
            <PostContainer />
        </div>
    );
};

export default Dash;